syntax = "proto3";
package meta;
// metamodel_version: 1.12.0
// an attributed description
message AltDescription {
  // the source of an attributed description
  string source = 1;
  // text of an attributed description
  string description = 2;
}
// mixin for classes that support annotations
message Annotatable {
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 1;
}
// a tag/value pair with the semantics of OWL Annotation
message Annotation {
  // a tag associated with an extension
  string tag = 1;
  // the actual annotation
  AnyValue value = 2;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 3;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 4;
}
message AnonymousClassExpression {
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 1;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 2;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 4;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 6;
  // Outstanding issues that needs resolution
  repeated string todos = 7;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 8;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 9;
  // example usages of an element
  repeated Example examples = 10;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 12;
  // id of the schema that defined the element
  string from_schema = 13;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 14;
  // A related resource from which the element is derived.
  string source = 15;
  // the primary language used in the sources
  string in_language = 16;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 17;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 18;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 19;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 20;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 21;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 22;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 23;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 24;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 25;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 26;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 27;
  // agent that created the element
  string created_by = 28;
  // agent that contributed to the element
  repeated string contributors = 29;
  // time at which the element was created
  string created_on = 30;
  // time at which the element was last updated
  string last_updated_on = 31;
  // agent that modified the element
  string modified_by = 32;
  // status of the element
  string status = 33;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 34;
  // Keywords or tags used to describe the element
  repeated string keywords = 35;
  // A class that any instance satisfying this expression must also be an instance of
  ClassDefinition is_a = 11;
  // holds if at least one of the expressions hold
  repeated AnonymousClassExpression any_of = 101;
  // holds if only one of the expressions hold
  repeated AnonymousClassExpression exactly_one_of = 103;
  // holds if none of the expressions hold
  repeated AnonymousClassExpression none_of = 105;
  // holds if all of the expressions hold
  repeated AnonymousClassExpression all_of = 107;
  // expresses constraints on a group of slots for a class expression
  repeated SlotDefinition slot_conditions = 36;
}
// An enum_expression that is not named
message AnonymousEnumExpression {
  // the identifier of an enumeration code set.
  string code_set = 1;
  // the version tag of the enumeration code set
  string code_set_tag = 2;
  // the version identifier of the enumeration code set
  string code_set_version = 3;
  // Defines the specific formula to be used to generate the permissible values.
  PvFormulaOptions pv_formula = 4;
  // A list of possible values for a slot range
  repeated PermissibleValue permissible_values = 5;
  // An enum expression that yields a list of permissible values that are to be included, after subtracting the minus set
  repeated AnonymousEnumExpression include = 6;
  // An enum expression that yields a list of permissible values that are to be subtracted from the enum
  repeated AnonymousEnumExpression minus = 7;
  // An enum definition that is used as the basis to create a new enum
  repeated EnumDefinition inherits = 8;
  // Specifies a query for obtaining a list of permissible values based on graph reachability
  ReachabilityQuery reachable_from = 9;
  // Specifies a match query that is used to calculate the list of permissible values
  MatchQuery matches = 10;
  // A list of identifiers that are used to construct a set of permissible values
  repeated string concepts = 11;
}
// An abstract parent class for any nested expression
message AnonymousExpression {
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 1;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 2;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 4;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 6;
  // Outstanding issues that needs resolution
  repeated string todos = 7;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 8;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 9;
  // example usages of an element
  repeated Example examples = 10;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 11;
  // id of the schema that defined the element
  string from_schema = 12;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 13;
  // A related resource from which the element is derived.
  string source = 14;
  // the primary language used in the sources
  string in_language = 15;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 16;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 17;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 18;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 19;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 20;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 21;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 22;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 23;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 24;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 25;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 26;
  // agent that created the element
  string created_by = 27;
  // agent that contributed to the element
  repeated string contributors = 28;
  // time at which the element was created
  string created_on = 29;
  // time at which the element was last updated
  string last_updated_on = 30;
  // agent that modified the element
  string modified_by = 31;
  // status of the element
  string status = 32;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 33;
  // Keywords or tags used to describe the element
  repeated string keywords = 34;
}
message AnonymousSlotExpression {
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 1;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 2;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 4;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 6;
  // Outstanding issues that needs resolution
  repeated string todos = 10;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 11;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 12;
  // example usages of an element
  repeated Example examples = 13;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 14;
  // id of the schema that defined the element
  string from_schema = 15;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 16;
  // A related resource from which the element is derived.
  string source = 17;
  // the primary language used in the sources
  string in_language = 18;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 19;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 20;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 21;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 22;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 23;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 24;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 26;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 28;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 29;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 30;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 31;
  // agent that created the element
  string created_by = 32;
  // agent that contributed to the element
  repeated string contributors = 33;
  // time at which the element was created
  string created_on = 34;
  // time at which the element was last updated
  string last_updated_on = 36;
  // agent that modified the element
  string modified_by = 37;
  // status of the element
  string status = 38;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 39;
  // Keywords or tags used to describe the element
  repeated string keywords = 40;
  // defines the type of the object of the slot.  Given the following slot definition
  //   S1:
  //     domain: C1
  //     range:  C2
  // the declaration
  //   X:
  //     S1: Y
  // 
  // implicitly asserts Y is an instance of C2
  // 
  Element range = 41;
  // A range that is described as a boolean expression combining existing ranges
  AnonymousClassExpression range_expression = 42;
  // An inlined enumeration
  EnumExpression enum_range = 43;
  // A collection of enum bindings that specify how a slot can be bound to a permissible value from an enumeration.
  // LinkML provides enums to allow string values to be restricted to one of a set of permissible values (specified statically or dynamically).
  // Enum bindings allow enums to be bound to any object, including complex nested objects. For example, given a (generic) class Concept with slots id and label, it may be desirable to restrict the values the id takes on in a given context. For example, a HumanSample class may have a slot for representing sample site, with a range of concept, but the values of that slot may be restricted to concepts from a particular branch of an anatomy ontology.
  repeated EnumBinding bindings = 44;
  // true means that the slot must be present in instances of the class definition
  bool required = 8;
  // true means that the slot should be present in instances of the class definition, but this is not required
  bool recommended = 9;
  // true means that slot can have more than one value and should be represented using a list or collection structure.
  bool multivalued = 7;
  // True means that keyed or identified slot appears in an outer structure by value.  False means that only the key or identifier for the slot appears within the domain, referencing a structure that appears elsewhere.
  bool inlined = 25;
  // True means that an inlined slot is represented as a list of range instances.  False means that an inlined slot is represented as a dictionary, whose key is the slot key or identifier and whose value is the range instance.
  bool inlined_as_list = 27;
  // For ordinal ranges, the value must be equal to or higher than this
  Anything minimum_value = 45;
  // For ordinal ranges, the value must be equal to or lower than this
  Anything maximum_value = 46;
  // the string value of the slot must conform to this regular expression expressed in the string
  string pattern = 35;
  // the string value of the slot must conform to the regular expression in the pattern expression
  PatternExpression structured_pattern = 47;
  // an encoding of a unit
  UnitOfMeasure unit = 48;
  // Causes the slot value to be interpreted as a uriorcurie after prefixing with this string
  string implicit_prefix = 49;
  // if PRESENT then a value must be present (for lists there must be at least one value). If ABSENT then a value must be absent (for lists, must be empty)
  PresenceEnum value_presence = 50;
  // the slot must have range string and the value of the slot must equal the specified value
  string equals_string = 52;
  // the slot must have range string and the value of the slot must equal one of the specified values
  repeated string equals_string_in = 53;
  // the slot must have range of a number and the value of the slot must equal the specified value
  int32 equals_number = 54;
  // the value of the slot must equal the value of the evaluated expression
  string equals_expression = 55;
  // the exact number of entries for a multivalued slot
  int32 exact_cardinality = 56;
  // the minimum number of entries for a multivalued slot
  int32 minimum_cardinality = 57;
  // the maximum number of entries for a multivalued slot
  int32 maximum_cardinality = 58;
  // the value of the slot is multivalued with at least one member satisfying the condition
  AnonymousSlotExpression has_member = 59;
  // the value of the slot is multivalued with all members satisfying the condition
  AnonymousSlotExpression all_members = 60;
  // holds if none of the expressions hold
  repeated AnonymousSlotExpression none_of = 105;
  // holds if only one of the expressions hold
  repeated AnonymousSlotExpression exactly_one_of = 103;
  // holds if at least one of the expressions hold
  repeated AnonymousSlotExpression any_of = 101;
  // holds if all of the expressions hold
  repeated AnonymousSlotExpression all_of = 107;
  // coerces the value of the slot into an array and defines the dimensions of that array
  ArrayExpression array = 61;
}
// A type expression that is not a top-level named type definition. Used for nesting.
message AnonymousTypeExpression {
  // the string value of the slot must conform to this regular expression expressed in the string
  string pattern = 35;
  // the string value of the slot must conform to the regular expression in the pattern expression
  PatternExpression structured_pattern = 1;
  // an encoding of a unit
  UnitOfMeasure unit = 2;
  // Causes the slot value to be interpreted as a uriorcurie after prefixing with this string
  string implicit_prefix = 3;
  // the slot must have range string and the value of the slot must equal the specified value
  string equals_string = 4;
  // the slot must have range string and the value of the slot must equal one of the specified values
  repeated string equals_string_in = 5;
  // the slot must have range of a number and the value of the slot must equal the specified value
  int32 equals_number = 6;
  // For ordinal ranges, the value must be equal to or higher than this
  Anything minimum_value = 7;
  // For ordinal ranges, the value must be equal to or lower than this
  Anything maximum_value = 8;
  // holds if none of the expressions hold
  repeated AnonymousTypeExpression none_of = 105;
  // holds if only one of the expressions hold
  repeated AnonymousTypeExpression exactly_one_of = 103;
  // holds if at least one of the expressions hold
  repeated AnonymousTypeExpression any_of = 101;
  // holds if all of the expressions hold
  repeated AnonymousTypeExpression all_of = 107;
}
message Anything {
}
message AnyValue {
}
// defines the dimensions of an array
message ArrayExpression {
  // exact number of dimensions in the array
  int32 exact_number_dimensions = 1;
  // minimum number of dimensions in the array
  int32 minimum_number_dimensions = 2;
  // maximum number of dimensions in the array, or False if explicitly no maximum. If this is unset, and an explicit list of dimensions are passed using dimensions, then this is interpreted as a closed list and the maximum_number_dimensions is the length of the dimensions list, unless this value is set to False
  Anything maximum_number_dimensions = 4;
  // definitions of each axis in the array
  repeated DimensionExpression dimensions = 6;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 7;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 8;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 9;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 10;
  // Outstanding issues that needs resolution
  repeated string todos = 11;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 12;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 13;
  // example usages of an element
  repeated Example examples = 14;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 15;
  // id of the schema that defined the element
  string from_schema = 16;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 17;
  // A related resource from which the element is derived.
  string source = 18;
  // the primary language used in the sources
  string in_language = 19;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 20;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 21;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 22;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 23;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 24;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 25;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 26;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 27;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 28;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 29;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 30;
  // agent that created the element
  string created_by = 31;
  // agent that contributed to the element
  repeated string contributors = 32;
  // time at which the element was created
  string created_on = 33;
  // time at which the element was last updated
  string last_updated_on = 34;
  // agent that modified the element
  string modified_by = 35;
  // status of the element
  string status = 36;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 37;
  // Keywords or tags used to describe the element
  repeated string keywords = 38;
}
// an element whose instances are complex objects that may have slot-value assignments
message ClassDefinition {
  // the unique name of the element within the context of the schema.  Name is combined with the default prefix to form the globally unique subject of the target class.
  string name = 1;
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 4;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 7;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 8;
  repeated LocalName local_names = 9;
  // An established standard to which the element conforms.
  string conforms_to = 10;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 12;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 14;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 15;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 16;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 17;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 18;
  // Outstanding issues that needs resolution
  repeated string todos = 20;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 21;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 22;
  // example usages of an element
  repeated Example examples = 24;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 25;
  // id of the schema that defined the element
  string from_schema = 26;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 27;
  // A related resource from which the element is derived.
  string source = 28;
  // the primary language used in the sources
  string in_language = 30;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 32;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 33;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 34;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 35;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 36;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 37;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 38;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 39;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 40;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 41;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 42;
  // agent that created the element
  string created_by = 43;
  // agent that contributed to the element
  repeated string contributors = 44;
  // time at which the element was created
  string created_on = 45;
  // time at which the element was last updated
  string last_updated_on = 46;
  // agent that modified the element
  string modified_by = 47;
  // status of the element
  string status = 48;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 49;
  // Keywords or tags used to describe the element
  repeated string keywords = 50;
  // Indicates the class or slot cannot be directly instantiated and is intended for grouping purposes.
  bool abstract = 52;
  // Indicates the class or slot is intended to be inherited from without being an is_a parent. mixins should not be inherited from using is_a, except by other mixins.
  bool mixin = 53;
  // The identifier of a "value set" -- a set of identifiers that form the possible values for the range of a slot. Note: this is different than 'subproperty_of' in that 'subproperty_of' is intended to be a single ontology term while 'values_from' is the identifier of an entire value set.  Additionally, this is different than an enumeration in that in an enumeration, the values of the enumeration are listed directly in the model itself. Setting this property on a slot does not guarantee an expansion of the ontological hierarchy into an enumerated list of possible values in every serialization of the model.
  repeated string values_from = 54;
  // Used on a slot that stores the string serialization of the containing object. The syntax follows python formatted strings, with slot names enclosed in {}s. These are expanded using the values of those slots.
  // We call the slot with the serialization the s-slot, the slots used in the {}s are v-slots. If both s-slots and v-slots are populated on an object then the value of the s-slot should correspond to the expansion.
  // Implementations of frameworks may choose to use this property to either (a) PARSE: implement automated normalizations by parsing denormalized strings into complex objects (b) GENERATE: implement automated to_string labeling of complex objects
  // For example, a Measurement class may have 3 fields: unit, value, and string_value. The string_value slot may have a string_serialization of {value}{unit} such that if unit=cm and value=2, the value of string_value shouldd be 2cm
  string string_serialization = 55;
  // collection of slot names that are applicable to a class
  repeated SlotDefinition slots = 19;
  // the refinement of a slot in the context of the containing class definition.
  repeated SlotDefinition slot_usage = 23;
  // Inline definition of slots
  repeated SlotDefinition attributes = 29;
  // URI of the class that provides a semantic interpretation of the element in a linked data context. The URI may come from any namespace and may be shared between schemas
  string class_uri = 2;
  // DEPRECATED -- rdfs:subClassOf to be emitted in OWL generation
  string subclass_of = 56;
  // indicates that the domain element consists exactly of the members of the element in the range.
  repeated ClassDefinition union_of = 57;
  // The combination of is a plus defining slots form a genus-differentia definition, or the set of necessary and sufficient conditions that can be transformed into an OWL equivalence axiom
  repeated SlotDefinition defining_slots = 58;
  // Indicates that this is the Container class which forms the root of the serialized document structure in tree serializations
  bool tree_root = 31;
  // A collection of named unique keys for this class. Such unique keys may be spread over several slots, which is why they are also called "compound keys". A unique key uniquely identifies instances of the class within a given container, meaning there cannot be two (or more) instances of the class with the same values for all the slots that make up the unique key.
  repeated UniqueKey unique_keys = 59;
  // the collection of rules that apply to all members of this class
  repeated ClassRule rules = 60;
  // The collection of classification rules that apply to all members of this class. Classification rules allow for automatically assigning the instantiated type of an instance.
  repeated AnonymousClassExpression classification_rules = 61;
  // if true then induced/mangled slot names are not created for class_usage and attributes
  bool slot_names_unique = 62;
  // true if this class represents a relationship rather than an entity
  bool represents_relationship = 63;
  // Two classes are disjoint if they have no instances in common, two slots are disjoint if they can never hold between the same two instances
  repeated ClassDefinition disjoint_with = 64;
  // If true then all direct is_a children are mutually disjoint and share no instances in common
  bool children_are_mutually_disjoint = 65;
  // How a class instance handles extra data not specified in the class definition.
  // Note that this does *not* define the constraints that are placed on additional slots defined by inheriting classes.
  // 
  // Possible values:
  // - `allowed: true` - allow all additional data
  // - `allowed: false` (or `allowed:` or `allowed: null` while `range_expression` is `null`) -
  //   forbid all additional data (default)
  // - `range_expression: ...`  - allow additional data if it matches the slot expression (see examples)
  // 
  ExtraSlotsExpression extra_slots = 66;
  // the name used for a slot in the context of its owning class.  If present, this is used instead of the actual slot name.
  string alias = 6;
  // A primary parent class from which inheritable metaslots are propagated
  ClassDefinition is_a = 11;
  // A collection of secondary parent mixin classes from which inheritable metaslots are propagated
  repeated ClassDefinition mixins = 13;
  // Used to extend class or slot definitions. For example, if we have a core schema where a gene has two slots for identifier and symbol, and we have a specialized schema for my_organism where we wish to add a slot systematic_name, we can avoid subclassing by defining a class gene_my_organism, adding the slot to this class, and then adding an apply_to pointing to the gene class. The new slot will be 'injected into' the gene class.
  repeated ClassDefinition apply_to = 67;
  // holds if at least one of the expressions hold
  repeated AnonymousClassExpression any_of = 101;
  // holds if only one of the expressions hold
  repeated AnonymousClassExpression exactly_one_of = 103;
  // holds if none of the expressions hold
  repeated AnonymousClassExpression none_of = 105;
  // holds if all of the expressions hold
  repeated AnonymousClassExpression all_of = 107;
  // expresses constraints on a group of slots for a class expression
  repeated SlotDefinition slot_conditions = 68;
}
// A boolean expression that can be used to dynamically determine membership of a class
message ClassExpression {
  // holds if at least one of the expressions hold
  repeated AnonymousClassExpression any_of = 101;
  // holds if only one of the expressions hold
  repeated AnonymousClassExpression exactly_one_of = 103;
  // holds if none of the expressions hold
  repeated AnonymousClassExpression none_of = 105;
  // holds if all of the expressions hold
  repeated AnonymousClassExpression all_of = 107;
  // expresses constraints on a group of slots for a class expression
  repeated SlotDefinition slot_conditions = 1;
}
// A rule that is applied to classes
message ClassLevelRule {
}
// A rule that applies to instances of a class
message ClassRule {
  // an expression that must hold in order for the rule to be applicable to an instance
  AnonymousClassExpression preconditions = 111;
  // an expression that must hold for an instance of the class, if the preconditions hold
  AnonymousClassExpression postconditions = 113;
  // an expression that must hold for an instance of the class, if the preconditions no not hold
  AnonymousClassExpression elseconditions = 115;
  // in addition to preconditions entailing postconditions, the postconditions entail the preconditions
  bool bidirectional = 1;
  // if true, the the postconditions may be omitted in instance data, but it is valid for an inference engine to add these
  bool open_world = 2;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // a deactivated rule is not executed by the rules engine
  bool deactivated = 4;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 6;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 7;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 8;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 9;
  // Outstanding issues that needs resolution
  repeated string todos = 10;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 11;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 12;
  // example usages of an element
  repeated Example examples = 13;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 14;
  // id of the schema that defined the element
  string from_schema = 15;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 16;
  // A related resource from which the element is derived.
  string source = 17;
  // the primary language used in the sources
  string in_language = 18;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 19;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 20;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 21;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 22;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 23;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 24;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 25;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 26;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 27;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 28;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 29;
  // agent that created the element
  string created_by = 30;
  // agent that contributed to the element
  repeated string contributors = 31;
  // time at which the element was created
  string created_on = 32;
  // time at which the element was last updated
  string last_updated_on = 33;
  // agent that modified the element
  string modified_by = 34;
  // status of the element
  string status = 35;
  // Controlled terms used to categorize an element.
  repeated string categories = 36;
  // Keywords or tags used to describe the element
  repeated string keywords = 37;
}
// Generic metadata shared across definitions
message CommonMetadata {
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 1;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 2;
  // Outstanding issues that needs resolution
  repeated string todos = 4;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 6;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 7;
  // example usages of an element
  repeated Example examples = 8;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 9;
  // id of the schema that defined the element
  string from_schema = 10;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 11;
  // A related resource from which the element is derived.
  string source = 12;
  // the primary language used in the sources
  string in_language = 13;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 14;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 15;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 16;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 17;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 18;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 19;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 20;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 21;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 22;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 23;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 24;
  // agent that created the element
  string created_by = 25;
  // agent that contributed to the element
  repeated string contributors = 26;
  // time at which the element was created
  string created_on = 27;
  // time at which the element was last updated
  string last_updated_on = 28;
  // agent that modified the element
  string modified_by = 29;
  // status of the element
  string status = 30;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 31;
  // Keywords or tags used to describe the element
  repeated string keywords = 32;
}
// abstract base class for core metaclasses
message Definition {
  // the unique name of the element within the context of the schema.  Name is combined with the default prefix to form the globally unique subject of the target class.
  string name = 1;
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 2;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 4;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 6;
  repeated LocalName local_names = 7;
  // An established standard to which the element conforms.
  string conforms_to = 8;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 9;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 10;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 12;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 14;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 15;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 16;
  // Outstanding issues that needs resolution
  repeated string todos = 17;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 18;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 19;
  // example usages of an element
  repeated Example examples = 20;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 21;
  // id of the schema that defined the element
  string from_schema = 22;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 23;
  // A related resource from which the element is derived.
  string source = 24;
  // the primary language used in the sources
  string in_language = 25;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 26;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 27;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 28;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 29;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 30;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 31;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 32;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 33;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 34;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 35;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 36;
  // agent that created the element
  string created_by = 37;
  // agent that contributed to the element
  repeated string contributors = 38;
  // time at which the element was created
  string created_on = 39;
  // time at which the element was last updated
  string last_updated_on = 40;
  // agent that modified the element
  string modified_by = 41;
  // status of the element
  string status = 42;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 43;
  // Keywords or tags used to describe the element
  repeated string keywords = 44;
  // A primary parent class or slot from which inheritable metaslots are propagated from. While multiple inheritance is not allowed, mixins can be provided effectively providing the same thing. The semantics are the same when translated to formalisms that allow MI (e.g. RDFS/OWL). When translating to a SI framework (e.g. java classes, python classes) then is a is used. When translating a framework without polymorphism (e.g. json-schema, solr document schema) then is a and mixins are recursively unfolded
  Definition is_a = 11;
  // Indicates the class or slot cannot be directly instantiated and is intended for grouping purposes.
  bool abstract = 45;
  // Indicates the class or slot is intended to be inherited from without being an is_a parent. mixins should not be inherited from using is_a, except by other mixins.
  bool mixin = 46;
  // A collection of secondary parent classes or slots from which inheritable metaslots are propagated from.
  repeated Definition mixins = 13;
  // Used to extend class or slot definitions. For example, if we have a core schema where a gene has two slots for identifier and symbol, and we have a specialized schema for my_organism where we wish to add a slot systematic_name, we can avoid subclassing by defining a class gene_my_organism, adding the slot to this class, and then adding an apply_to pointing to the gene class. The new slot will be 'injected into' the gene class.
  repeated Definition apply_to = 47;
  // The identifier of a "value set" -- a set of identifiers that form the possible values for the range of a slot. Note: this is different than 'subproperty_of' in that 'subproperty_of' is intended to be a single ontology term while 'values_from' is the identifier of an entire value set.  Additionally, this is different than an enumeration in that in an enumeration, the values of the enumeration are listed directly in the model itself. Setting this property on a slot does not guarantee an expansion of the ontological hierarchy into an enumerated list of possible values in every serialization of the model.
  repeated string values_from = 48;
  // Used on a slot that stores the string serialization of the containing object. The syntax follows python formatted strings, with slot names enclosed in {}s. These are expanded using the values of those slots.
  // We call the slot with the serialization the s-slot, the slots used in the {}s are v-slots. If both s-slots and v-slots are populated on an object then the value of the s-slot should correspond to the expansion.
  // Implementations of frameworks may choose to use this property to either (a) PARSE: implement automated normalizations by parsing denormalized strings into complex objects (b) GENERATE: implement automated to_string labeling of complex objects
  // For example, a Measurement class may have 3 fields: unit, value, and string_value. The string_value slot may have a string_serialization of {value}{unit} such that if unit=cm and value=2, the value of string_value shouldd be 2cm
  string string_serialization = 49;
}
// defines one of the dimensions of an array
message DimensionExpression {
  // the name used for a slot in the context of its owning class.  If present, this is used instead of the actual slot name.
  string alias = 6;
  // the maximum number of entries for a multivalued slot
  int32 maximum_cardinality = 1;
  // the minimum number of entries for a multivalued slot
  int32 minimum_cardinality = 2;
  // the exact number of entries for a multivalued slot
  int32 exact_cardinality = 4;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 7;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 8;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 9;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 10;
  // Outstanding issues that needs resolution
  repeated string todos = 11;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 12;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 13;
  // example usages of an element
  repeated Example examples = 14;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 15;
  // id of the schema that defined the element
  string from_schema = 16;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 17;
  // A related resource from which the element is derived.
  string source = 18;
  // the primary language used in the sources
  string in_language = 19;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 20;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 21;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 22;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 23;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 24;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 25;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 26;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 27;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 28;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 29;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 30;
  // agent that created the element
  string created_by = 31;
  // agent that contributed to the element
  repeated string contributors = 32;
  // time at which the element was created
  string created_on = 33;
  // time at which the element was last updated
  string last_updated_on = 34;
  // agent that modified the element
  string modified_by = 35;
  // status of the element
  string status = 36;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 37;
  // Keywords or tags used to describe the element
  repeated string keywords = 38;
}
// A named element in the model
message Element {
  // the unique name of the element within the context of the schema.  Name is combined with the default prefix to form the globally unique subject of the target class.
  string name = 1;
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 2;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 4;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 6;
  repeated LocalName local_names = 7;
  // An established standard to which the element conforms.
  string conforms_to = 8;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 9;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 10;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 11;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 12;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 13;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 14;
  // Outstanding issues that needs resolution
  repeated string todos = 15;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 16;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 17;
  // example usages of an element
  repeated Example examples = 18;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 19;
  // id of the schema that defined the element
  string from_schema = 20;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 21;
  // A related resource from which the element is derived.
  string source = 22;
  // the primary language used in the sources
  string in_language = 23;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 24;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 25;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 26;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 27;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 28;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 29;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 30;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 31;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 32;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 33;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 34;
  // agent that created the element
  string created_by = 35;
  // agent that contributed to the element
  repeated string contributors = 36;
  // time at which the element was created
  string created_on = 37;
  // time at which the element was last updated
  string last_updated_on = 38;
  // agent that modified the element
  string modified_by = 39;
  // status of the element
  string status = 40;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 41;
  // Keywords or tags used to describe the element
  repeated string keywords = 42;
}
// A binding of a slot or a class to a permissible value from an enumeration.
message EnumBinding {
  // defines the type of the object of the slot.  Given the following slot definition
  //   S1:
  //     domain: C1
  //     range:  C2
  // the declaration
  //   X:
  //     S1: Y
  // 
  // implicitly asserts Y is an instance of C2
  // 
  EnumDefinition range = 1;
  // The level of obligation or recommendation strength for a metadata element
  ObligationLevelEnum obligation_level = 2;
  // A path to a slot that is being bound to a permissible value from an enumeration.
  string binds_value_of = 4;
  // Defines the specific formula to be used to generate the permissible values.
  PvFormulaOptions pv_formula = 6;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 7;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 8;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 9;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 10;
  // Outstanding issues that needs resolution
  repeated string todos = 11;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 12;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 13;
  // example usages of an element
  repeated Example examples = 14;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 15;
  // id of the schema that defined the element
  string from_schema = 16;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 17;
  // A related resource from which the element is derived.
  string source = 18;
  // the primary language used in the sources
  string in_language = 19;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 20;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 21;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 22;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 23;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 24;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 25;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 26;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 27;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 28;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 29;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 30;
  // agent that created the element
  string created_by = 31;
  // agent that contributed to the element
  repeated string contributors = 32;
  // time at which the element was created
  string created_on = 33;
  // time at which the element was last updated
  string last_updated_on = 34;
  // agent that modified the element
  string modified_by = 35;
  // status of the element
  string status = 36;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 37;
  // Keywords or tags used to describe the element
  repeated string keywords = 38;
}
// an element whose instances must be drawn from a specified set of permissible values
message EnumDefinition {
  // the unique name of the element within the context of the schema.  Name is combined with the default prefix to form the globally unique subject of the target class.
  string name = 1;
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 2;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 4;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 6;
  repeated LocalName local_names = 7;
  // An established standard to which the element conforms.
  string conforms_to = 8;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 9;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 10;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 12;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 14;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 15;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 16;
  // Outstanding issues that needs resolution
  repeated string todos = 17;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 18;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 19;
  // example usages of an element
  repeated Example examples = 20;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 21;
  // id of the schema that defined the element
  string from_schema = 22;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 23;
  // A related resource from which the element is derived.
  string source = 24;
  // the primary language used in the sources
  string in_language = 25;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 26;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 27;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 28;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 29;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 30;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 31;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 32;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 33;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 34;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 35;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 36;
  // agent that created the element
  string created_by = 37;
  // agent that contributed to the element
  repeated string contributors = 38;
  // time at which the element was created
  string created_on = 39;
  // time at which the element was last updated
  string last_updated_on = 40;
  // agent that modified the element
  string modified_by = 41;
  // status of the element
  string status = 42;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 43;
  // Keywords or tags used to describe the element
  repeated string keywords = 44;
  // A primary parent class or slot from which inheritable metaslots are propagated from. While multiple inheritance is not allowed, mixins can be provided effectively providing the same thing. The semantics are the same when translated to formalisms that allow MI (e.g. RDFS/OWL). When translating to a SI framework (e.g. java classes, python classes) then is a is used. When translating a framework without polymorphism (e.g. json-schema, solr document schema) then is a and mixins are recursively unfolded
  Definition is_a = 11;
  // Indicates the class or slot cannot be directly instantiated and is intended for grouping purposes.
  bool abstract = 45;
  // Indicates the class or slot is intended to be inherited from without being an is_a parent. mixins should not be inherited from using is_a, except by other mixins.
  bool mixin = 46;
  // A collection of secondary parent classes or slots from which inheritable metaslots are propagated from.
  repeated Definition mixins = 13;
  // Used to extend class or slot definitions. For example, if we have a core schema where a gene has two slots for identifier and symbol, and we have a specialized schema for my_organism where we wish to add a slot systematic_name, we can avoid subclassing by defining a class gene_my_organism, adding the slot to this class, and then adding an apply_to pointing to the gene class. The new slot will be 'injected into' the gene class.
  repeated Definition apply_to = 47;
  // The identifier of a "value set" -- a set of identifiers that form the possible values for the range of a slot. Note: this is different than 'subproperty_of' in that 'subproperty_of' is intended to be a single ontology term while 'values_from' is the identifier of an entire value set.  Additionally, this is different than an enumeration in that in an enumeration, the values of the enumeration are listed directly in the model itself. Setting this property on a slot does not guarantee an expansion of the ontological hierarchy into an enumerated list of possible values in every serialization of the model.
  repeated string values_from = 48;
  // Used on a slot that stores the string serialization of the containing object. The syntax follows python formatted strings, with slot names enclosed in {}s. These are expanded using the values of those slots.
  // We call the slot with the serialization the s-slot, the slots used in the {}s are v-slots. If both s-slots and v-slots are populated on an object then the value of the s-slot should correspond to the expansion.
  // Implementations of frameworks may choose to use this property to either (a) PARSE: implement automated normalizations by parsing denormalized strings into complex objects (b) GENERATE: implement automated to_string labeling of complex objects
  // For example, a Measurement class may have 3 fields: unit, value, and string_value. The string_value slot may have a string_serialization of {value}{unit} such that if unit=cm and value=2, the value of string_value shouldd be 2cm
  string string_serialization = 49;
  // URI of the enum that provides a semantic interpretation of the element in a linked data context. The URI may come from any namespace and may be shared between schemas
  string enum_uri = 50;
  // the identifier of an enumeration code set.
  string code_set = 52;
  // the version tag of the enumeration code set
  string code_set_tag = 53;
  // the version identifier of the enumeration code set
  string code_set_version = 54;
  // Defines the specific formula to be used to generate the permissible values.
  PvFormulaOptions pv_formula = 55;
  // A list of possible values for a slot range
  repeated PermissibleValue permissible_values = 56;
  // An enum expression that yields a list of permissible values that are to be included, after subtracting the minus set
  repeated AnonymousEnumExpression include = 57;
  // An enum expression that yields a list of permissible values that are to be subtracted from the enum
  repeated AnonymousEnumExpression minus = 58;
  // An enum definition that is used as the basis to create a new enum
  repeated EnumDefinition inherits = 59;
  // Specifies a query for obtaining a list of permissible values based on graph reachability
  ReachabilityQuery reachable_from = 60;
  // Specifies a match query that is used to calculate the list of permissible values
  MatchQuery matches = 61;
  // A list of identifiers that are used to construct a set of permissible values
  repeated string concepts = 62;
}
// An expression that constrains the range of a slot
message EnumExpression {
  // the identifier of an enumeration code set.
  string code_set = 1;
  // the version tag of the enumeration code set
  string code_set_tag = 2;
  // the version identifier of the enumeration code set
  string code_set_version = 3;
  // Defines the specific formula to be used to generate the permissible values.
  PvFormulaOptions pv_formula = 4;
  // A list of possible values for a slot range
  repeated PermissibleValue permissible_values = 5;
  // An enum expression that yields a list of permissible values that are to be included, after subtracting the minus set
  repeated AnonymousEnumExpression include = 6;
  // An enum expression that yields a list of permissible values that are to be subtracted from the enum
  repeated AnonymousEnumExpression minus = 7;
  // An enum definition that is used as the basis to create a new enum
  repeated EnumDefinition inherits = 8;
  // Specifies a query for obtaining a list of permissible values based on graph reachability
  ReachabilityQuery reachable_from = 9;
  // Specifies a match query that is used to calculate the list of permissible values
  MatchQuery matches = 10;
  // A list of identifiers that are used to construct a set of permissible values
  repeated string concepts = 11;
}
// usage example and description
message Example {
  // example value
  string value = 1;
  // description of what the value is doing
  string description = 2;
  // direct object representation of the example
  Anything object = 3;
}
// general mixin for any class that can represent some form of expression
message Expression {
}
// mixin for classes that support extension
message Extensible {
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 1;
}
// a tag/value pair used to add non-model information to an entry
message Extension {
  // a tag associated with an extension
  string tag = 1;
  // the actual annotation
  AnyValue value = 2;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 3;
}
// An expression that defines how to handle additional data in an instance of class
// beyond the slots/attributes defined for that class.
// See `extra_slots` for usage examples.
// 
message ExtraSlotsExpression {
  // Whether or not something is allowed. Usage defined by context.
  bool allowed = 1;
  // A range that is described as a boolean expression combining existing ranges
  AnonymousSlotExpression range_expression = 2;
}
// an expression describing an import
message ImportExpression {
  string import_from = 1;
  string import_as = 2;
  repeated Setting import_map = 4;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 6;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 7;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 8;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 9;
  // Outstanding issues that needs resolution
  repeated string todos = 10;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 11;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 12;
  // example usages of an element
  repeated Example examples = 13;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 14;
  // id of the schema that defined the element
  string from_schema = 15;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 16;
  // A related resource from which the element is derived.
  string source = 17;
  // the primary language used in the sources
  string in_language = 18;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 19;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 20;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 21;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 22;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 23;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 24;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 25;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 26;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 27;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 28;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 29;
  // agent that created the element
  string created_by = 30;
  // agent that contributed to the element
  repeated string contributors = 31;
  // time at which the element was created
  string created_on = 32;
  // time at which the element was last updated
  string last_updated_on = 33;
  // agent that modified the element
  string modified_by = 34;
  // status of the element
  string status = 35;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 36;
  // Keywords or tags used to describe the element
  repeated string keywords = 37;
}
// an attributed label
message LocalName {
  // the ncname of the source of the name
  string local_name_source = 1;
  // a name assigned to an element in a given ontology
  string local_name_value = 2;
}
// A query that is used on an enum expression to dynamically obtain a set of permissible values via a query that matches on properties of the external concepts.
message MatchQuery {
  // A regular expression that is used to obtain a set of identifiers from a source_ontology to construct a set of permissible values
  string identifier_pattern = 1;
  // An ontology or vocabulary or terminology that is used in a query to obtain a set of permissible values
  string source_ontology = 2;
}
// An expression that describes an abstract path from an object to another through a sequence of slot lookups
message PathExpression {
  // in a sequential list, this indicates the next member
  PathExpression followed_by = 1;
  // holds if none of the expressions hold
  repeated PathExpression none_of = 105;
  // holds if at least one of the expressions hold
  repeated PathExpression any_of = 101;
  // holds if all of the expressions hold
  repeated PathExpression all_of = 107;
  // holds if only one of the expressions hold
  repeated PathExpression exactly_one_of = 103;
  // true if the slot is to be inversed
  bool reversed = 2;
  // the slot to traverse
  SlotDefinition traverse = 4;
  // A range that is described as a boolean expression combining existing ranges
  AnonymousClassExpression range_expression = 6;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 7;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 8;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 9;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 10;
  // Outstanding issues that needs resolution
  repeated string todos = 11;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 12;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 13;
  // example usages of an element
  repeated Example examples = 14;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 15;
  // id of the schema that defined the element
  string from_schema = 16;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 17;
  // A related resource from which the element is derived.
  string source = 18;
  // the primary language used in the sources
  string in_language = 19;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 20;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 21;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 22;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 23;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 24;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 25;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 26;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 27;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 28;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 29;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 30;
  // agent that created the element
  string created_by = 31;
  // agent that contributed to the element
  repeated string contributors = 32;
  // time at which the element was created
  string created_on = 33;
  // time at which the element was last updated
  string last_updated_on = 34;
  // agent that modified the element
  string modified_by = 35;
  // status of the element
  string status = 36;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 37;
  // Keywords or tags used to describe the element
  repeated string keywords = 38;
}
// a regular expression pattern used to evaluate conformance of a string
message PatternExpression {
  // the string value of the slot must conform to this regular expression expressed in the string. May be interpolated.
  string syntax = 1;
  // if true then the pattern is first string interpolated
  bool interpolated = 2;
  // if not true then the pattern must match the whole string, as if enclosed in ^...$
  bool partial_match = 4;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 6;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 7;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 8;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 9;
  // Outstanding issues that needs resolution
  repeated string todos = 10;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 11;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 12;
  // example usages of an element
  repeated Example examples = 13;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 14;
  // id of the schema that defined the element
  string from_schema = 15;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 16;
  // A related resource from which the element is derived.
  string source = 17;
  // the primary language used in the sources
  string in_language = 18;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 19;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 20;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 21;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 22;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 23;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 24;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 25;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 26;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 27;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 28;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 29;
  // agent that created the element
  string created_by = 30;
  // agent that contributed to the element
  repeated string contributors = 31;
  // time at which the element was created
  string created_on = 32;
  // time at which the element was last updated
  string last_updated_on = 33;
  // agent that modified the element
  string modified_by = 34;
  // status of the element
  string status = 35;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 36;
  // Keywords or tags used to describe the element
  repeated string keywords = 37;
}
// a permissible value, accompanied by intended text and an optional mapping to a concept URI
message PermissibleValue {
  // The actual permissible value itself
  string text = 21;
  // a textual description of the element's purpose and use
  string description = 5;
  // the value meaning of a permissible value
  string meaning = 23;
  // an encoding of a unit
  UnitOfMeasure unit = 1;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 2;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 4;
  // A primary parent class or slot from which inheritable metaslots are propagated from. While multiple inheritance is not allowed, mixins can be provided effectively providing the same thing. The semantics are the same when translated to formalisms that allow MI (e.g. RDFS/OWL). When translating to a SI framework (e.g. java classes, python classes) then is a is used. When translating a framework without polymorphism (e.g. json-schema, solr document schema) then is a and mixins are recursively unfolded
  PermissibleValue is_a = 11;
  // A collection of secondary parent classes or slots from which inheritable metaslots are propagated from.
  repeated PermissibleValue mixins = 13;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 6;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 7;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 8;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 9;
  // Outstanding issues that needs resolution
  repeated string todos = 10;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 12;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 14;
  // example usages of an element
  repeated Example examples = 15;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 16;
  // id of the schema that defined the element
  string from_schema = 17;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 18;
  // A related resource from which the element is derived.
  string source = 19;
  // the primary language used in the sources
  string in_language = 20;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 22;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 24;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 25;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 26;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 27;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 28;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 29;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 30;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 31;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 32;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 33;
  // agent that created the element
  string created_by = 34;
  // agent that contributed to the element
  repeated string contributors = 35;
  // time at which the element was created
  string created_on = 36;
  // time at which the element was last updated
  string last_updated_on = 37;
  // agent that modified the element
  string modified_by = 38;
  // status of the element
  string status = 39;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 40;
  // Keywords or tags used to describe the element
  repeated string keywords = 41;
}
// prefix URI tuple
message Prefix {
  // The prefix components of a prefix expansions. This is the part that appears before the colon in a CURIE.
  string prefix_prefix = 1;
  // The namespace to which a prefix expands to.
  string prefix_reference = 2;
}
// A query that is used on an enum expression to dynamically obtain a set of permissible values via walking from a set of source nodes to a set of descendants or ancestors over a set of relationship types.
message ReachabilityQuery {
  // An ontology or vocabulary or terminology that is used in a query to obtain a set of permissible values
  string source_ontology = 1;
  // A list of nodes that are used in the reachability query
  repeated string source_nodes = 2;
  // A list of relationship types (properties) that are used in a reachability query
  repeated string relationship_types = 3;
  // True if the reachability query should only include directly related nodes, if False then include also transitively connected
  bool is_direct = 4;
  // True if the query is reflexive
  bool include_self = 5;
  // True if the direction of the reachability query is reversed and ancestors are retrieved
  bool traverse_up = 6;
}
// A collection of definitions that make up a schema or a data model.
message SchemaDefinition {
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 2;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 7;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 9;
  repeated LocalName local_names = 12;
  // An established standard to which the element conforms.
  string conforms_to = 14;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 15;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 16;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 17;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 18;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 19;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 22;
  // Outstanding issues that needs resolution
  repeated string todos = 23;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 24;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 25;
  // example usages of an element
  repeated Example examples = 26;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 27;
  // id of the schema that defined the element
  string from_schema = 28;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 29;
  // A related resource from which the element is derived.
  string source = 30;
  // the primary language used in the sources
  string in_language = 32;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 33;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 34;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 35;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 36;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 37;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 38;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 39;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 40;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 41;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 42;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 43;
  // agent that created the element
  string created_by = 44;
  // agent that contributed to the element
  repeated string contributors = 45;
  // time at which the element was created
  string created_on = 46;
  // time at which the element was last updated
  string last_updated_on = 47;
  // agent that modified the element
  string modified_by = 48;
  // status of the element
  string status = 49;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 50;
  // Keywords or tags used to describe the element
  repeated string keywords = 52;
  // The official schema URI
  string id = 53;
  // particular version of schema
  string version = 54;
  // A list of schemas that are to be included in this schema
  repeated string imports = 21;
  // license for the schema
  string license = 31;
  // A collection of prefix expansions that specify how CURIEs can be expanded to URIs
  repeated Prefix prefixes = 10;
  // a list of Curie prefixes that are used in the representation of instances of the model.  All prefixes in this list are added to the prefix sections of the target models.
  repeated string emit_prefixes = 55;
  // ordered list of prefixcommon biocontexts to be fetched to resolve id prefixes and inline prefix variables
  repeated string default_curi_maps = 56;
  // The prefix that is used for all elements within a schema
  string default_prefix = 11;
  // default slot range to be used if range element is omitted from a slot definition
  TypeDefinition default_range = 13;
  // An index to the collection of all subset definitions in the schema
  repeated SubsetDefinition subsets = 8;
  // An index to the collection of all type definitions in the schema
  repeated TypeDefinition types = 6;
  // An index to the collection of all enum definitions in the schema
  repeated EnumDefinition enums = 5;
  // An index to the collection of all slot definitions in the schema
  repeated SlotDefinition slots = 4;
  // An index to the collection of all class definitions in the schema
  repeated ClassDefinition classes = 3;
  // Version of the metamodel used to load the schema
  string metamodel_version = 57;
  // name, uri or description of the source of the schema
  string source_file = 58;
  // modification date of the source of the schema
  string source_file_date = 59;
  // size in bytes of the source of the schema
  int32 source_file_size = 60;
  // date and time that the schema was loaded/generated
  string generation_date = 61;
  // if true then induced/mangled slot names are not created for class_usage and attributes
  bool slot_names_unique = 62;
  // A collection of global variable settings
  repeated Setting settings = 20;
  // A collection of enum bindings that specify how a slot can be bound to a permissible value from an enumeration.
  // LinkML provides enums to allow string values to be restricted to one of a set of permissible values (specified statically or dynamically).
  // Enum bindings allow enums to be bound to any object, including complex nested objects. For example, given a (generic) class Concept with slots id and label, it may be desirable to restrict the values the id takes on in a given context. For example, a HumanSample class may have a slot for representing sample site, with a range of concept, but the values of that slot may be restricted to concepts from a particular branch of an anatomy ontology.
  repeated EnumBinding bindings = 63;
  // a unique name for the schema that is both human-readable and consists of only characters from the NCName set
  string name = 1;
}
// assignment of a key to a value
message Setting {
  // the variable name for a setting
  string setting_key = 1;
  // The value assigned for a setting
  string setting_value = 2;
}
// an element that describes how instances are related to other instances
message SlotDefinition {
  // the unique name of the element within the context of the schema.  Name is combined with the default prefix to form the globally unique subject of the target class.
  string name = 1;
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 4;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 10;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 12;
  repeated LocalName local_names = 14;
  // An established standard to which the element conforms.
  string conforms_to = 15;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 16;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 17;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 18;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 19;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 20;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 21;
  // Outstanding issues that needs resolution
  repeated string todos = 22;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 23;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 24;
  // example usages of an element
  repeated Example examples = 26;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 28;
  // id of the schema that defined the element
  string from_schema = 29;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 30;
  // A related resource from which the element is derived.
  string source = 31;
  // the primary language used in the sources
  string in_language = 32;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 33;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 34;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 36;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 37;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 38;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 39;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 40;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 41;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 42;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 43;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 44;
  // agent that created the element
  string created_by = 45;
  // agent that contributed to the element
  repeated string contributors = 46;
  // time at which the element was created
  string created_on = 47;
  // time at which the element was last updated
  string last_updated_on = 48;
  // agent that modified the element
  string modified_by = 49;
  // status of the element
  string status = 50;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 52;
  // Keywords or tags used to describe the element
  repeated string keywords = 53;
  // Indicates the class or slot cannot be directly instantiated and is intended for grouping purposes.
  bool abstract = 54;
  // Indicates the class or slot is intended to be inherited from without being an is_a parent. mixins should not be inherited from using is_a, except by other mixins.
  bool mixin = 55;
  // The identifier of a "value set" -- a set of identifiers that form the possible values for the range of a slot. Note: this is different than 'subproperty_of' in that 'subproperty_of' is intended to be a single ontology term while 'values_from' is the identifier of an entire value set.  Additionally, this is different than an enumeration in that in an enumeration, the values of the enumeration are listed directly in the model itself. Setting this property on a slot does not guarantee an expansion of the ontological hierarchy into an enumerated list of possible values in every serialization of the model.
  repeated string values_from = 56;
  // Used on a slot that stores the string serialization of the containing object. The syntax follows python formatted strings, with slot names enclosed in {}s. These are expanded using the values of those slots.
  // We call the slot with the serialization the s-slot, the slots used in the {}s are v-slots. If both s-slots and v-slots are populated on an object then the value of the s-slot should correspond to the expansion.
  // Implementations of frameworks may choose to use this property to either (a) PARSE: implement automated normalizations by parsing denormalized strings into complex objects (b) GENERATE: implement automated to_string labeling of complex objects
  // For example, a Measurement class may have 3 fields: unit, value, and string_value. The string_value slot may have a string_serialization of {value}{unit} such that if unit=cm and value=2, the value of string_value shouldd be 2cm
  string string_serialization = 57;
  // a name that is used in the singular form
  string singular_name = 58;
  // defines the type of the subject of the slot.  Given the following slot definition
  //   S1:
  //     domain: C1
  //     range:  C2
  // the declaration
  //   X:
  //     S1: Y
  // 
  // implicitly asserts that X is an instance of C1
  // 
  ClassDefinition domain = 59;
  // URI of the class that provides a semantic interpretation of the slot in a linked data context. The URI may come from any namespace and may be shared between schemas.
  string slot_uri = 2;
  // true means that the *value* of a slot is inherited by subclasses
  bool inherited = 60;
  // If present, slot is read only.  Text explains why
  string readonly = 61;
  // function that provides a default value for the slot.
  //   * [Tt]rue -- boolean True
  //   * [Ff]alse -- boolean False
  //   * bnode -- blank node identifier
  //   * class_curie -- CURIE for the containing class
  //   * class_uri -- URI for the containing class
  //   * default_ns -- schema default namespace
  //   * default_range -- schema default range
  //   * int(value) -- integer value
  //   * slot_uri -- URI for the slot
  //   * slot_curie -- CURIE for the slot
  //   * string(value) -- string value
  //   * EnumName(PermissibleValue) -- enum value
  string ifabsent = 62;
  // If True, then there must be no duplicates in the elements of a multivalued slot
  bool list_elements_unique = 63;
  // If True, then the order of elements of a multivalued slot is guaranteed to be preserved. If False, the order may still be preserved but this is not guaranteed
  bool list_elements_ordered = 64;
  // If True, then the relationship between the slot domain and range is many to one or many to many
  bool shared = 65;
  // True means that the slot is the "singular unique key" (also known more simply as the "key slot") of its class. Such a slot uniquely identifies instances of the class within a single container, meaning there cannot be two (or more) instances of the class (or instances of any of its descendants) with the same value for the key slot within the container.
  bool key = 66;
  // True means that the slot is the identifier slot of its class. Such a slot uniquely identifies instances of the class throughout an entire document, meaning there cannot be two (or more) instances of the class (or instances of any of its descendants) with the same value for the identifier slot anywhere in the document.
  bool identifier = 5;
  // True means that the key slot(s) is used to determine the instantiation (types) relation between objects and a ClassDefinition
  bool designates_type = 67;
  // the name used for a slot in the context of its owning class.  If present, this is used instead of the actual slot name.
  string alias = 6;
  // the "owner" of the slot. It is the class if it appears in the slots list, otherwise the declaring slot
  Definition owner = 68;
  // the class(es) that reference the slot in a "slots" or "slot_usage" context
  repeated ClassDefinition domain_of = 69;
  // Ontology property which this slot is a subproperty of.  Note: setting this property on a slot does not guarantee an expansion of the ontological hierarchy into an enumerated list of possible values in every serialization of the model.
  SlotDefinition subproperty_of = 70;
  // If s is symmetric, and i.s=v, then v.s=i
  bool symmetric = 71;
  // If s is reflexive, then i.s=i for all instances i
  bool reflexive = 72;
  // If s is locally_reflexive, then i.s=i for all instances i where s is a class slot for the type of i
  bool locally_reflexive = 73;
  // If s is irreflexive, then there exists no i such i.s=i
  bool irreflexive = 74;
  // If s is antisymmetric, and i.s=v where i is different from v, v.s cannot have value i
  bool asymmetric = 75;
  // If s is transitive, and i.s=z, and s.s=j, then i.s=j
  bool transitive = 76;
  // indicates that any instance of d s r implies that there is also an instance of r s' d
  SlotDefinition inverse = 77;
  // indicates that for any instance, i, the domain of this slot will include an assertion of i s range
  bool is_class_field = 78;
  // If s transitive_form_of d, then (1) s holds whenever d holds (2) s is transitive (3) d holds whenever s holds and there are no intermediates, and s is not reflexive
  SlotDefinition transitive_form_of = 79;
  // transitive_form_of including the reflexive case
  SlotDefinition reflexive_transitive_form_of = 80;
  // a textual descriptor that indicates the role played by the slot range
  string role = 81;
  // True means that this slot was defined in a slot_usage situation
  bool is_usage_slot = 82;
  // The name of the slot referenced in the slot_usage
  string usage_slot_name = 83;
  // the role a slot on a relationship class plays, for example, the subject, object or predicate roles
  RelationalRoleEnum relational_role = 84;
  // allows for grouping of related slots into a grouping slot that serves the role of a group
  SlotDefinition slot_group = 85;
  // true if this slot is a grouping slot
  bool is_grouping_slot = 86;
  // a rule for inferring a slot assignment based on evaluating a path through a sequence of slot assignments
  PathExpression path_rule = 87;
  // Two classes are disjoint if they have no instances in common, two slots are disjoint if they can never hold between the same two instances
  repeated SlotDefinition disjoint_with = 88;
  // If true then all direct is_a children are mutually disjoint and share no instances in common
  bool children_are_mutually_disjoint = 89;
  // indicates that the domain element consists exactly of the members of the element in the range.
  repeated SlotDefinition union_of = 90;
  // A collection of type mappings that specify how a slot's range should be mapped or serialized in different frameworks
  repeated TypeMapping type_mappings = 91;
  // A primary parent slot from which inheritable metaslots are propagated
  SlotDefinition is_a = 11;
  // A collection of secondary parent mixin slots from which inheritable metaslots are propagated
  repeated SlotDefinition mixins = 13;
  // Used to extend class or slot definitions. For example, if we have a core schema where a gene has two slots for identifier and symbol, and we have a specialized schema for my_organism where we wish to add a slot systematic_name, we can avoid subclassing by defining a class gene_my_organism, adding the slot to this class, and then adding an apply_to pointing to the gene class. The new slot will be 'injected into' the gene class.
  repeated SlotDefinition apply_to = 92;
  // defines the type of the object of the slot.  Given the following slot definition
  //   S1:
  //     domain: C1
  //     range:  C2
  // the declaration
  //   X:
  //     S1: Y
  // 
  // implicitly asserts Y is an instance of C2
  // 
  Element range = 93;
  // A range that is described as a boolean expression combining existing ranges
  AnonymousClassExpression range_expression = 94;
  // An inlined enumeration
  EnumExpression enum_range = 95;
  // A collection of enum bindings that specify how a slot can be bound to a permissible value from an enumeration.
  // LinkML provides enums to allow string values to be restricted to one of a set of permissible values (specified statically or dynamically).
  // Enum bindings allow enums to be bound to any object, including complex nested objects. For example, given a (generic) class Concept with slots id and label, it may be desirable to restrict the values the id takes on in a given context. For example, a HumanSample class may have a slot for representing sample site, with a range of concept, but the values of that slot may be restricted to concepts from a particular branch of an anatomy ontology.
  repeated EnumBinding bindings = 96;
  // true means that the slot must be present in instances of the class definition
  bool required = 8;
  // true means that the slot should be present in instances of the class definition, but this is not required
  bool recommended = 9;
  // true means that slot can have more than one value and should be represented using a list or collection structure.
  bool multivalued = 7;
  // True means that keyed or identified slot appears in an outer structure by value.  False means that only the key or identifier for the slot appears within the domain, referencing a structure that appears elsewhere.
  bool inlined = 25;
  // True means that an inlined slot is represented as a list of range instances.  False means that an inlined slot is represented as a dictionary, whose key is the slot key or identifier and whose value is the range instance.
  bool inlined_as_list = 27;
  // For ordinal ranges, the value must be equal to or higher than this
  Anything minimum_value = 97;
  // For ordinal ranges, the value must be equal to or lower than this
  Anything maximum_value = 98;
  // the string value of the slot must conform to this regular expression expressed in the string
  string pattern = 35;
  // the string value of the slot must conform to the regular expression in the pattern expression
  PatternExpression structured_pattern = 99;
  // an encoding of a unit
  UnitOfMeasure unit = 100;
  // Causes the slot value to be interpreted as a uriorcurie after prefixing with this string
  string implicit_prefix = 102;
  // if PRESENT then a value must be present (for lists there must be at least one value). If ABSENT then a value must be absent (for lists, must be empty)
  PresenceEnum value_presence = 104;
  // the slot must have range string and the value of the slot must equal the specified value
  string equals_string = 106;
  // the slot must have range string and the value of the slot must equal one of the specified values
  repeated string equals_string_in = 108;
  // the slot must have range of a number and the value of the slot must equal the specified value
  int32 equals_number = 109;
  // the value of the slot must equal the value of the evaluated expression
  string equals_expression = 110;
  // the exact number of entries for a multivalued slot
  int32 exact_cardinality = 111;
  // the minimum number of entries for a multivalued slot
  int32 minimum_cardinality = 112;
  // the maximum number of entries for a multivalued slot
  int32 maximum_cardinality = 113;
  // the value of the slot is multivalued with at least one member satisfying the condition
  AnonymousSlotExpression has_member = 114;
  // the value of the slot is multivalued with all members satisfying the condition
  AnonymousSlotExpression all_members = 115;
  // holds if none of the expressions hold
  repeated AnonymousSlotExpression none_of = 105;
  // holds if only one of the expressions hold
  repeated AnonymousSlotExpression exactly_one_of = 103;
  // holds if at least one of the expressions hold
  repeated AnonymousSlotExpression any_of = 101;
  // holds if all of the expressions hold
  repeated AnonymousSlotExpression all_of = 107;
  // coerces the value of the slot into an array and defines the dimensions of that array
  ArrayExpression array = 116;
}
// an expression that constrains the range of values a slot can take
message SlotExpression {
  // defines the type of the object of the slot.  Given the following slot definition
  //   S1:
  //     domain: C1
  //     range:  C2
  // the declaration
  //   X:
  //     S1: Y
  // 
  // implicitly asserts Y is an instance of C2
  // 
  Element range = 1;
  // A range that is described as a boolean expression combining existing ranges
  AnonymousClassExpression range_expression = 2;
  // An inlined enumeration
  EnumExpression enum_range = 3;
  // A collection of enum bindings that specify how a slot can be bound to a permissible value from an enumeration.
  // LinkML provides enums to allow string values to be restricted to one of a set of permissible values (specified statically or dynamically).
  // Enum bindings allow enums to be bound to any object, including complex nested objects. For example, given a (generic) class Concept with slots id and label, it may be desirable to restrict the values the id takes on in a given context. For example, a HumanSample class may have a slot for representing sample site, with a range of concept, but the values of that slot may be restricted to concepts from a particular branch of an anatomy ontology.
  repeated EnumBinding bindings = 4;
  // true means that the slot must be present in instances of the class definition
  bool required = 8;
  // true means that the slot should be present in instances of the class definition, but this is not required
  bool recommended = 9;
  // true means that slot can have more than one value and should be represented using a list or collection structure.
  bool multivalued = 7;
  // True means that keyed or identified slot appears in an outer structure by value.  False means that only the key or identifier for the slot appears within the domain, referencing a structure that appears elsewhere.
  bool inlined = 25;
  // True means that an inlined slot is represented as a list of range instances.  False means that an inlined slot is represented as a dictionary, whose key is the slot key or identifier and whose value is the range instance.
  bool inlined_as_list = 27;
  // For ordinal ranges, the value must be equal to or higher than this
  Anything minimum_value = 5;
  // For ordinal ranges, the value must be equal to or lower than this
  Anything maximum_value = 6;
  // the string value of the slot must conform to this regular expression expressed in the string
  string pattern = 35;
  // the string value of the slot must conform to the regular expression in the pattern expression
  PatternExpression structured_pattern = 10;
  // an encoding of a unit
  UnitOfMeasure unit = 11;
  // Causes the slot value to be interpreted as a uriorcurie after prefixing with this string
  string implicit_prefix = 12;
  // if PRESENT then a value must be present (for lists there must be at least one value). If ABSENT then a value must be absent (for lists, must be empty)
  PresenceEnum value_presence = 13;
  // the slot must have range string and the value of the slot must equal the specified value
  string equals_string = 14;
  // the slot must have range string and the value of the slot must equal one of the specified values
  repeated string equals_string_in = 15;
  // the slot must have range of a number and the value of the slot must equal the specified value
  int32 equals_number = 16;
  // the value of the slot must equal the value of the evaluated expression
  string equals_expression = 17;
  // the exact number of entries for a multivalued slot
  int32 exact_cardinality = 18;
  // the minimum number of entries for a multivalued slot
  int32 minimum_cardinality = 19;
  // the maximum number of entries for a multivalued slot
  int32 maximum_cardinality = 20;
  // the value of the slot is multivalued with at least one member satisfying the condition
  AnonymousSlotExpression has_member = 21;
  // the value of the slot is multivalued with all members satisfying the condition
  AnonymousSlotExpression all_members = 22;
  // holds if none of the expressions hold
  repeated AnonymousSlotExpression none_of = 105;
  // holds if only one of the expressions hold
  repeated AnonymousSlotExpression exactly_one_of = 103;
  // holds if at least one of the expressions hold
  repeated AnonymousSlotExpression any_of = 101;
  // holds if all of the expressions hold
  repeated AnonymousSlotExpression all_of = 107;
  // coerces the value of the slot into an array and defines the dimensions of that array
  ArrayExpression array = 23;
}
// object that contains meta data about a synonym or alias including where it came from (source) and its scope (narrow, broad, etc.)
message StructuredAlias {
  // The literal lexical form of a structured alias; i.e the actual alias value.
  string literal_form = 1;
  // The relationship between an element and its alias.
  AliasPredicateEnum predicate = 2;
  // The category or categories of an alias. This can be drawn from any relevant vocabulary
  repeated string categories = 4;
  // The context in which an alias should be applied
  repeated string contexts = 6;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 7;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 8;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 9;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 10;
  // Outstanding issues that needs resolution
  repeated string todos = 11;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 12;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 13;
  // example usages of an element
  repeated Example examples = 14;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 15;
  // id of the schema that defined the element
  string from_schema = 16;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 17;
  // A related resource from which the element is derived.
  string source = 18;
  // the primary language used in the sources
  string in_language = 19;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 20;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 21;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 22;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 23;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 24;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 25;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 26;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 27;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 28;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 29;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 30;
  // agent that created the element
  string created_by = 31;
  // agent that contributed to the element
  repeated string contributors = 32;
  // time at which the element was created
  string created_on = 33;
  // time at which the element was last updated
  string last_updated_on = 34;
  // agent that modified the element
  string modified_by = 35;
  // status of the element
  string status = 36;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Keywords or tags used to describe the element
  repeated string keywords = 37;
}
// an element that can be used to group other metamodel elements
message SubsetDefinition {
  // the unique name of the element within the context of the schema.  Name is combined with the default prefix to form the globally unique subject of the target class.
  string name = 1;
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 2;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 4;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 6;
  repeated LocalName local_names = 7;
  // An established standard to which the element conforms.
  string conforms_to = 8;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 9;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 10;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 11;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 12;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 13;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 14;
  // Outstanding issues that needs resolution
  repeated string todos = 15;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 16;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 17;
  // example usages of an element
  repeated Example examples = 18;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 19;
  // id of the schema that defined the element
  string from_schema = 20;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 21;
  // A related resource from which the element is derived.
  string source = 22;
  // the primary language used in the sources
  string in_language = 23;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 24;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 25;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 26;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 27;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 28;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 29;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 30;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 31;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 32;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 33;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 34;
  // agent that created the element
  string created_by = 35;
  // agent that contributed to the element
  repeated string contributors = 36;
  // time at which the element was created
  string created_on = 37;
  // time at which the element was last updated
  string last_updated_on = 38;
  // agent that modified the element
  string modified_by = 39;
  // status of the element
  string status = 40;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 41;
  // Keywords or tags used to describe the element
  repeated string keywords = 42;
}
// an element that whose instances are atomic scalar values that can be mapped to primitive types
message TypeDefinition {
  // the unique name of the element within the context of the schema.  Name is combined with the default prefix to form the globally unique subject of the target class.
  string name = 1;
  // An allowed list of prefixes for which identifiers must conform. The identifier of this class or slot must begin with the URIs referenced by this prefix
  repeated string id_prefixes = 4;
  // If true, then the id_prefixes slot is treated as being closed, and any use of an id that does not have this prefix is considered a violation.
  bool id_prefixes_are_closed = 6;
  // The native URI of the element. This is always within the namespace of the containing schema. Contrast with the assigned URI, via class_uri or slot_uri
  string definition_uri = 9;
  repeated LocalName local_names = 11;
  // An established standard to which the element conforms.
  string conforms_to = 12;
  // An element in another schema which this element conforms to. The referenced element is not imported into the schema for the implementing element. However, the referenced schema may be used to check conformance of the implementing element.
  repeated string implements = 13;
  // An element in another schema which this element instantiates.
  repeated string instantiates = 14;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 15;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 16;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 17;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 18;
  // Outstanding issues that needs resolution
  repeated string todos = 19;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 20;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 21;
  // example usages of an element
  repeated Example examples = 22;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 23;
  // id of the schema that defined the element
  string from_schema = 24;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 25;
  // A related resource from which the element is derived.
  string source = 26;
  // the primary language used in the sources
  string in_language = 27;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 28;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 29;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 30;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 31;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 32;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 33;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 34;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 36;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 37;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 38;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 39;
  // agent that created the element
  string created_by = 40;
  // agent that contributed to the element
  repeated string contributors = 41;
  // time at which the element was created
  string created_on = 42;
  // time at which the element was last updated
  string last_updated_on = 43;
  // agent that modified the element
  string modified_by = 44;
  // status of the element
  string status = 45;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 46;
  // Keywords or tags used to describe the element
  repeated string keywords = 47;
  // A parent type from which type properties are inherited
  TypeDefinition typeof = 7;
  // python base type in the LinkML runtime that implements this type definition
  string base = 8;
  // The uri that defines the possible values for the type definition
  string uri = 2;
  // the name of the python object that implements this type definition
  string repr = 10;
  // indicates that the domain element consists exactly of the members of the element in the range.
  repeated TypeDefinition union_of = 48;
  // the string value of the slot must conform to this regular expression expressed in the string
  string pattern = 35;
  // the string value of the slot must conform to the regular expression in the pattern expression
  PatternExpression structured_pattern = 49;
  // an encoding of a unit
  UnitOfMeasure unit = 50;
  // Causes the slot value to be interpreted as a uriorcurie after prefixing with this string
  string implicit_prefix = 52;
  // the slot must have range string and the value of the slot must equal the specified value
  string equals_string = 53;
  // the slot must have range string and the value of the slot must equal one of the specified values
  repeated string equals_string_in = 54;
  // the slot must have range of a number and the value of the slot must equal the specified value
  int32 equals_number = 55;
  // For ordinal ranges, the value must be equal to or higher than this
  Anything minimum_value = 56;
  // For ordinal ranges, the value must be equal to or lower than this
  Anything maximum_value = 57;
  // holds if none of the expressions hold
  repeated AnonymousTypeExpression none_of = 105;
  // holds if only one of the expressions hold
  repeated AnonymousTypeExpression exactly_one_of = 103;
  // holds if at least one of the expressions hold
  repeated AnonymousTypeExpression any_of = 101;
  // holds if all of the expressions hold
  repeated AnonymousTypeExpression all_of = 107;
}
// An abstract class grouping named types and anonymous type expressions
message TypeExpression {
  // the string value of the slot must conform to this regular expression expressed in the string
  string pattern = 35;
  // the string value of the slot must conform to the regular expression in the pattern expression
  PatternExpression structured_pattern = 1;
  // an encoding of a unit
  UnitOfMeasure unit = 2;
  // Causes the slot value to be interpreted as a uriorcurie after prefixing with this string
  string implicit_prefix = 3;
  // the slot must have range string and the value of the slot must equal the specified value
  string equals_string = 4;
  // the slot must have range string and the value of the slot must equal one of the specified values
  repeated string equals_string_in = 5;
  // the slot must have range of a number and the value of the slot must equal the specified value
  int32 equals_number = 6;
  // For ordinal ranges, the value must be equal to or higher than this
  Anything minimum_value = 7;
  // For ordinal ranges, the value must be equal to or lower than this
  Anything maximum_value = 8;
  // holds if none of the expressions hold
  repeated AnonymousTypeExpression none_of = 105;
  // holds if only one of the expressions hold
  repeated AnonymousTypeExpression exactly_one_of = 103;
  // holds if at least one of the expressions hold
  repeated AnonymousTypeExpression any_of = 101;
  // holds if all of the expressions hold
  repeated AnonymousTypeExpression all_of = 107;
}
// Represents how a slot or type can be serialized to a format.
message TypeMapping {
  // The name of a format that can be used to serialize LinkML data. The string value should be a code from the LinkML frameworks vocabulary, but this is not strictly enforced
  string framework = 1;
  // type to coerce to
  TypeDefinition type = 2;
  // Used on a slot that stores the string serialization of the containing object. The syntax follows python formatted strings, with slot names enclosed in {}s. These are expanded using the values of those slots.
  // We call the slot with the serialization the s-slot, the slots used in the {}s are v-slots. If both s-slots and v-slots are populated on an object then the value of the s-slot should correspond to the expansion.
  // Implementations of frameworks may choose to use this property to either (a) PARSE: implement automated normalizations by parsing denormalized strings into complex objects (b) GENERATE: implement automated to_string labeling of complex objects
  // For example, a Measurement class may have 3 fields: unit, value, and string_value. The string_value slot may have a string_serialization of {value}{unit} such that if unit=cm and value=2, the value of string_value shouldd be 2cm
  string string_serialization = 4;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 6;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 7;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 8;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 9;
  // Outstanding issues that needs resolution
  repeated string todos = 10;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 11;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 12;
  // example usages of an element
  repeated Example examples = 13;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 14;
  // id of the schema that defined the element
  string from_schema = 15;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 16;
  // A related resource from which the element is derived.
  string source = 17;
  // the primary language used in the sources
  string in_language = 18;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 19;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 20;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 21;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 22;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 23;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 24;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 25;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 26;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 27;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 28;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 29;
  // agent that created the element
  string created_by = 30;
  // agent that contributed to the element
  repeated string contributors = 31;
  // time at which the element was created
  string created_on = 32;
  // time at which the element was last updated
  string last_updated_on = 33;
  // agent that modified the element
  string modified_by = 34;
  // status of the element
  string status = 35;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 36;
  // Keywords or tags used to describe the element
  repeated string keywords = 37;
}
// a collection of slots whose values uniquely identify an instance of a class
message UniqueKey {
  // name of the unique key
  string unique_key_name = 1;
  // list of slot names that form a key. The tuple formed from the values of all these slots should be unique.
  repeated SlotDefinition unique_key_slots = 2;
  // By default, None values are considered equal for the purposes of comparisons in determining uniqueness. Set this to true to treat missing values as per ANSI-SQL NULLs, i.e NULL=NULL is always False.
  bool consider_nulls_inequal = 4;
  // a tag/text tuple attached to an arbitrary element
  repeated Extension extensions = 6;
  // a collection of tag/text tuples with the semantics of OWL Annotation
  repeated Annotation annotations = 7;
  // a textual description of the element's purpose and use
  string description = 5;
  // A sourced alternative description for an element
  repeated AltDescription alt_descriptions = 8;
  // A concise human-readable display label for the element. The title should mirror the name, and should use ordinary textual punctuation.
  string title = 3;
  // Description of why and when this element will no longer be used
  string deprecated = 9;
  // Outstanding issues that needs resolution
  repeated string todos = 10;
  // editorial notes about an element intended primarily for internal consumption
  repeated string notes = 11;
  // notes and comments about an element intended primarily for external consumption
  repeated string comments = 12;
  // example usages of an element
  repeated Example examples = 13;
  // used to indicate membership of a term in a defined subset of terms used for a particular domain or application.
  repeated SubsetDefinition in_subset = 14;
  // id of the schema that defined the element
  string from_schema = 15;
  // the imports entry that this element was derived from.  Empty means primary source
  string imported_from = 16;
  // A related resource from which the element is derived.
  string source = 17;
  // the primary language used in the sources
  string in_language = 18;
  // A list of related entities or URLs that may be of relevance
  repeated string see_also = 19;
  // When an element is deprecated, it can be automatically replaced by this uri or curie
  string deprecated_element_has_exact_replacement = 20;
  // When an element is deprecated, it can be potentially replaced by this uri or curie
  string deprecated_element_has_possible_replacement = 21;
  // Alternate names/labels for the element. These do not alter the semantics of the schema, but may be useful to support search and alignment.
  repeated string aliases = 22;
  // A list of structured_alias objects, used to provide aliases in conjunction with additional metadata.
  repeated StructuredAlias structured_aliases = 23;
  // A list of terms from different schemas or terminology systems that have comparable meaning. These may include terms that are precisely equivalent, broader or narrower in meaning, or otherwise semantically related but not equivalent from a strict ontological perspective.
  repeated string mappings = 24;
  // A list of terms from different schemas or terminology systems that have identical meaning.
  repeated string exact_mappings = 25;
  // A list of terms from different schemas or terminology systems that have close meaning.
  repeated string close_mappings = 26;
  // A list of terms from different schemas or terminology systems that have related meaning.
  repeated string related_mappings = 27;
  // A list of terms from different schemas or terminology systems that have narrower meaning.
  repeated string narrow_mappings = 28;
  // A list of terms from different schemas or terminology systems that have broader meaning.
  repeated string broad_mappings = 29;
  // agent that created the element
  string created_by = 30;
  // agent that contributed to the element
  repeated string contributors = 31;
  // time at which the element was created
  string created_on = 32;
  // time at which the element was last updated
  string last_updated_on = 33;
  // agent that modified the element
  string modified_by = 34;
  // status of the element
  string status = 35;
  // the relative order in which the element occurs, lower values are given precedence
  int32 rank = 51;
  // Controlled terms used to categorize an element.
  repeated string categories = 36;
  // Keywords or tags used to describe the element
  repeated string keywords = 37;
}
// A unit of measure, or unit, is a particular quantity value that has been chosen as a scale for measuring other quantities the same kind (more generally of equivalent dimension).
message UnitOfMeasure {
  // name of the unit encoded as a symbol
  string symbol = 1;
  // An abbreviation for a unit is a short ASCII string that is used in place of the full name for the unit in contexts where non-ASCII characters would be problematic, or where using the abbreviation will enhance readability. When a power of a base unit needs to be expressed, such as squares this can be done using abbreviations rather than symbols (source: qudt)
  string abbreviation = 2;
  // the spelled out name of the unit, for example, meter
  string descriptive_name = 3;
  // Used to link a unit to equivalent concepts in ontologies such as UO, SNOMED, OEM, OBOE, NCIT
  repeated string exact_mappings = 4;
  // associates a QUDT unit with its UCUM code (case-sensitive).
  string ucum_code = 5;
  // Expression for deriving this unit from other units
  string derivation = 6;
  // Concept in a vocabulary or ontology that denotes the kind of quantity being measured, e.g. length
  string has_quantity_kind = 7;
  string iec61360code = 8;
}

// permissible values for the relationship between an element and an alias
enum AliasPredicateEnum {
  ALIAS_PREDICATE_ENUM_UNSPECIFIED = 0;
  ALIAS_PREDICATE_ENUM_EXACT_SYNONYM = 1;
  ALIAS_PREDICATE_ENUM_RELATED_SYNONYM = 2;
  ALIAS_PREDICATE_ENUM_BROAD_SYNONYM = 3;
  ALIAS_PREDICATE_ENUM_NARROW_SYNONYM = 4;
}
// The level of obligation or recommendation strength for a metadata element
enum ObligationLevelEnum {
  OBLIGATION_LEVEL_ENUM_UNSPECIFIED = 0;
  OBLIGATION_LEVEL_ENUM_REQUIRED = 1;
  OBLIGATION_LEVEL_ENUM_RECOMMENDED = 2;
  OBLIGATION_LEVEL_ENUM_OPTIONAL = 3;
  OBLIGATION_LEVEL_ENUM_EXAMPLE = 4;
  OBLIGATION_LEVEL_ENUM_DISCOURAGED = 5;
}
// enumeration of conditions by which a slot value should be set
enum PresenceEnum {
  PRESENCE_ENUM_UNSPECIFIED = 0;
  PRESENCE_ENUM_UNCOMMITTED = 1;
  PRESENCE_ENUM_PRESENT = 2;
  PRESENCE_ENUM_ABSENT = 3;
}
// The formula used to generate the set of permissible values from the code_set values
enum PvFormulaOptions {
  PV_FORMULA_OPTIONS_UNSPECIFIED = 0;
  PV_FORMULA_OPTIONS_CODE = 1;
  PV_FORMULA_OPTIONS_CURIE = 2;
  PV_FORMULA_OPTIONS_URI = 3;
  PV_FORMULA_OPTIONS_FHIR_CODING = 4;
  PV_FORMULA_OPTIONS_LABEL = 5;
}
// enumeration of roles a slot on a relationship class can play
enum RelationalRoleEnum {
  RELATIONAL_ROLE_ENUM_UNSPECIFIED = 0;
  RELATIONAL_ROLE_ENUM_SUBJECT = 1;
  RELATIONAL_ROLE_ENUM_OBJECT = 2;
  RELATIONAL_ROLE_ENUM_PREDICATE = 3;
  RELATIONAL_ROLE_ENUM_NODE = 4;
  RELATIONAL_ROLE_ENUM_OTHER_ROLE = 5;
}
